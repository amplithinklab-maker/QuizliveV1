services:
  - type: web
    name: livequiz-server
    runtime: node
    buildCommand: npm install --include=dev && npm run build -w server
    startCommand: npm start -w server
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
